import React from 'react'
import {View} from 'react-native'

const Hr = (props) => (
  <View
    style={{
      borderBottomColor: '#DCDCDC',
      borderBottomWidth: 1,
      marginRight: 8,
      marginLeft: 8,
      marginBottom: 4,
    }}
  />
);

export default Hr