import React from 'react'
import {Spinner as SpinnerNB} from 'native-base'

import colors from '../constants/colors'

const Spinner = () => (
  <SpinnerNB color={colors.secondary} />
);

export default Spinner